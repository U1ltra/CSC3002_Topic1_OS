#ifndef PAIR_H
#define PAIR_H
class Pair{
public:
    int lb;        //lower bound of the memory slice
    int ub;        //upper bound of the memory slice
    Pair(int a, int b);
};

#endif // PAIR_H
